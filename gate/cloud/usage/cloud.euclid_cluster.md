# cloud.euclid_cluster · Usage

| 接 | 调 | 看 |
| --- | --- | --- |
| `in: cloud{xyz}` → `out: list[cloud]` | `tolerance`/`min_size`/`max_size` | 接 MAP `over`；用例 12 |

门禁实现（内核验收）。改「最小点数」请绑 MAP body 的 `min_points`，勿改本算子 `min_size`。
