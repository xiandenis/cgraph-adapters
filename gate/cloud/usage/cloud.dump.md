# cloud.dump

| connect | tune | inspect |
| :--- | :--- | :--- |
| Wire `cloud` into `in`; read file handle from `out`. | `params.format` must be `ply` or `pcd`. | Artifact under `workdir/outputs/out`. |
