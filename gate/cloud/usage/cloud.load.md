# cloud.load

| connect | tune | inspect |
| :--- | :--- | :--- |
| Wire a ply/pcd file into `in`; read `cloud{xyz}` from `out`. | `params.format` optional; empty uses path suffix. | `out` is JSON SoA with `fields=[xyz]`. |
