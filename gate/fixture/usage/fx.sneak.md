# fx.sneak

| 接 | 调 | 看 |
| :--- | :--- | :--- |
| `in: file` → `out: file`。**test-only**，不得进 Studio 调色板。 | `undeclared: string`（未声明路径）。 | 故意读未声明路径；沙箱必须 failed。未声明写 `undeclared_extra` 不进 realisation。 |
