# vendor/rtr/

Wrappers for [`real_time_registration`](https://github.com/xiandenis/real_time_registration) as a **third-party** dependency (`find_package` / installed prefix).

Do **not** copy that repository into this tree.
